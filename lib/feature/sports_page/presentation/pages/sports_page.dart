import 'package:flutter/widgets.dart';

class SprtsPage extends StatelessWidget {
  const SprtsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sprts Page', style: TextStyle(fontSize: 24)),
    );
  }
}
